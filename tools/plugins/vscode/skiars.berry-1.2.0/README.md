## Berry VSCode Extension

This extension provides Berry language support for:
- TextMate syntax highlighting
- Semantic highlighting via a document semantic token provider

## Requirements

- Visual Studio Code `1.74.0` or newer

The minimum VSCode version is set to `1.74.0` because semantic token provider APIs are required for semantic highlighting support.